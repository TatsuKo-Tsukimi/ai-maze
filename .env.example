# === AI Maze Game 配置 ===

# 方式 1: OpenClaw 用户（推荐，零配置）
# 如果你已安装 OpenClaw 并完成认证，游戏会自动读取 auth-profiles.json 中的凭据。
# 无需额外配置。

# 方式 2: Anthropic API key
# ANTHROPIC_API_KEY=sk-ant-...

# 方式 3: OpenAI API key（也支持 Kimi / 智谱等 OpenAI 兼容 API）
# OPENAI_API_KEY=sk-...
# API_BASE=https://api.openai.com/v1

# 通用兜底（可选）
# 如果你只想放一个 API_KEY，程序会自动判断是 Anthropic 还是 OpenAI 风格。
# API_KEY=sk-...

# 模型覆盖（可选）
# MODEL=claude-sonnet-4-20250514
# MAZE_MODEL=claude-sonnet-4-20250514

# Soul / 记忆路径
# 指向你自己的 OpenClaw / Claude / agent workspace
SOUL_PATH=/path/to/your/openclaw/workspace

# 额外记忆路径（可选；Linux/macOS 用 : 分隔，Windows 用 ; 分隔）
# EXTRA_MEMORY_PATH=/path/to/your/memory-or-test-soul

# 可选：帮助自动检测本地 OpenClaw
# OPENCLAW_HOME=/path/to/.openclaw
# AUTH_PROFILE=anthropic:manual

# 其他可选配置
# PLAYER_NAME=YourName
# PORT=3000
# HOST=127.0.0.1
# DEBUG=1
